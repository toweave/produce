The `tooltip` component has been added. Remember to wrap your app with the `TooltipProvider` component.

```tsx title="app/layout.tsx"
import { TooltipProvider } from "@/components/ui/tooltip"

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>
        <TooltipProvider>{children}</TooltipProvider>
      </body>
    </html>
  )
}
```


1. 修复 src/renderer/src/components/layout.tsx 的 SidebarTrigger 问题，完善展开、缩略的事件。
2. 所有的滚动条 改为 thin.


/product 分析 @doc/product/seedance_15 下面的文档, 在项目中新建一个菜单组：Seedance-1.5，下面有子菜单：视频创作、任务列表等功能。
任务：
1. 目标是使用 Doubao-Seedance-1.5-pro 创作视频。
2. 分析 @doc/product/seedance_15 下面的文档,根据最佳实践完善产品开发需求。
3. 根据最佳实践完成这些需求：创建视频生成任务、取消或删除视频生成任务、查询视频生成任务、查询视频生成任务列表



文档先保存在 @doc/product/seedance_20，命名 {nnn}_{name}.md
然后调用 /developer 开始实现，可将本 PRD 作为输入开发对应功能
补充开发环境、生产环境密钥（暂时先用同一个）
# See Dance API 密钥（生产环境）
VITE_SEE_DANCE_API_KEY=63864453-1384-4e78-8f13-8d92c296adf0


/product 分析 @doc/product/seedance_20 下面的文档, 在项目中新建一个菜单组：Seedance-2.0，下面有子菜单：视频创作、任务列表等功能。
任务：
1. 目标是使用 Doubao-Seedance-2.0 创作视频。
2. 分析 @doc/product/seedance_20 下面的文档,根据最佳实践完善产品开发需求。
3. 根据最佳实践完成这些需求：创建视频生成任务、取消或删除视频生成任务、查询视频生成任务、查询视频生成任务列表
4. 生成的文档先保存在 @doc/product/seedance_20，命名 {nnn}_{name}.md


分析本项目，帮我添加 SQLite (better-sqlite3) 做数据存储，比如
1. 生成 Seedance-1.5 视频操作日志
2. 生成 Seedance-2.0 视频操作日志


##
现在把 Seedance-1.5 视频创作页面修改左右布局，各占 50% 的空间。完成如下任务：
1. 在左栏中 生成视频 按钮的右边显示 生成视频存储位置，用户可以自定义添加。控件有 select, list. 其中 list 显示用户添加过所有存储视频的位置。
  select 默认选择用户最近一次存储的视频位置，如果用户没有添加，系统自动初始化一个有效目标，默认系统的 downloads.
  select 控件支持用户自定义添加目录，添加目录即选择该存储目录。
2. 右栏中，上半区域显示当前生成好的视频，并添加用户点击生成视频功能时的加载状态。
  下半区域，当视频生成完成时，生成该视频 6 张关键帧，分为开头、其他是中间的平均位置、结尾。
   生成好的视频，支持播放暂停，当用户鼠标浮动到视频上面时，会出现“生成关键帧”的按钮，该功能时用户点击“生成关键帧”按钮的那一刻，视频播放的帧。
   当用户点击暂停视频，“生成关键帧”按钮一直存在，播放时消失（如果鼠标处于视频上浮动时，仍然不消失）
3. 生成的关键帧存放位置根据右侧选择的位置存放，同时追加在 6 张关键帧后面显示。
4. 视频上面显示 01:12:24 时间帧描述，同时视频暂停后，支持上下快捷键，切换前一帧、后一帧。
5. 如果 video 插件不够最优，可以考虑使用 xgplayer。
6. 生成的文档先保存在 @doc/product/seedance_15，命名 {nnn}_{name}.md

##
Seedance-1.5 的视频创作页面
1. 每次进入页面，右侧视频默认显示最近一次生成的视频，下面的帧数据也是同样（如有有历史数据直接使用）;
2. 宽高比默认显示 16:9, 时长默认为自动, 分辨率默认 1080p
3. 生成关键帧的速度特别慢，分析原因并解决。


Seedance-1.5 的视频创作页面，完成任务：
1. 每次进入页面，右侧视频默认显示最近一次生成的视频（生成视频成功后，应该存储替换任务值），下面的帧数据也是同样（如有有历史数据直接使用），视频的任务id 先使用 cgt-20260524203622-l66fm;
2. 生成关键帧的速度特别慢，分析原因并解决。
3. 右栏中，上半区域显示当前生成好的视频点击播放没有反应，分析是不是 xgplayer 的原因，如果是请替换合适的视频播放、处理插件。

每次进入页面，右侧视频默认显示最近一次生成的视频，这个任务还是不正确。参考任务列表数据，里面的预览功能
列表数据：cgt-20260524203622-l66fm	已完成	doubao-seedance-1-5-pro-251215	2026/5/24 20:36:28

帮重新规划秘密,值先用同一个
VITE_SEE_DANCE_15_KEY=63864453-1384-4e78-8f13-8d92c296adf0
VITE_SEE_DANCE_20_KEY=63864453-1384-4e78-8f13-8d92c296adf0
在点击用户时，添加栏目密钥管理，可以为不同模块添加密钥如上内容，用户相关的配置生成 setting.json, 在那里可以看到相关配置，例如：主题、密钥、个人信息
